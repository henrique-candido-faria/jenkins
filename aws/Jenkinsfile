pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                    aws emr list-clusters \
                        --region us-east-1 \
                        --active \
                        --profile socialminer
                '''
            }
        }
    }
}