pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh ("""
                    ../aws/emr/list-clusters.sh
                """)
            }
        }
    }
}