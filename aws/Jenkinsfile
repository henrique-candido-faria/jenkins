pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh ("""
                    emr/list-clusters.sh
                """)
            }
        }
    }
}